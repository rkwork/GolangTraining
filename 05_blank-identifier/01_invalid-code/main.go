package main

import "fmt"

func main() {
	a := "stored in a"
	// uncommenting the below line does't run the code
	// b := "stored in b"
	fmt.Println("a - ", a)
	// b is not being used - invalid code
}
